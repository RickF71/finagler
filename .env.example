# DIS-Core backend endpoint
VITE_API_BASE=http://localhost:8080

# Finagler deployment mode:
#   "finagler" - Default mode, rewrites /api/* to VITE_API_BASE
#   "finaglernone" - None Space mode, no rewriting (uses absolute URLs)
VITE_FINAGLER_MODE=finagler

# Usage Notes:
# ------------
# FINAGLER MODE (default):
#   - All relative /api/* requests are rewritten to ${VITE_API_BASE}/api/*
#   - Use this mode when Finagler is served from Vite dev server (port 5173)
#   - Or when Finagler is behind a reverse proxy that doesn't pass /api to dis-core
#
# FINAGLERNONE MODE:
#   - No URL rewriting occurs
#   - Components must use absolute URLs: fetch("http://localhost:8080/api/ping")
#   - Use this mode when Finagler is served directly from dis-core
#   - Or when running in a docker/k8s environment with service discovery
#
# Example configurations:
# -----------------------
# Dev environment (Vite on :5173, dis-core on :8080):
#   VITE_FINAGLER_MODE=finagler
#   VITE_API_BASE=http://localhost:8080
#
# None Space (dis-core serves Finagler):
#   VITE_FINAGLER_MODE=finaglernone
#   VITE_API_BASE=http://localhost:8080
#
# Production (reverse proxy):
#   VITE_FINAGLER_MODE=finagler
#   VITE_API_BASE=https://dis-core.example.com
